<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.GroovyShopping" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.GroovyShopping" parent="Base.Theme.GroovyShopping" />

    <style name="BaseBottomSheetDialog" parent="@style/Theme.Design.Light.BottomSheetDialog">
        <item name="android:windowIsFloating">false</item>
        <item name="bottomSheetStyle">@style/BottomSheet</item>
    </style>

    <style name="BottomSheet" parent="@style/Widget.Design.BottomSheet.Modal">
        <!--        <item name="android:background">@drawable/rounded_top_white</item>-->
        <!--        <item name="android:layout_marginStart">@dimen/margin_10</item>-->
        <!--        <item name="android:layout_marginEnd">@dimen/margin_10</item>-->
        <item name="behavior_peekHeight">@dimen/_550sdp</item>
    </style>



</resources>